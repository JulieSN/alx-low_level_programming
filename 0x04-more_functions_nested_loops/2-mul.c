#include "main.h"

/**
 * mul - function multiplying two integer
 * @i: input one
 * @j: input two
 * Return: Multiple of i and j
 */

int mul(int i, int j);
int mul(int i, int j)
{
	return (i * j);
}
